const CreateClient = require('./createClient');
const DeleteClient = require('./deleteClient');
const UpdateClient = require('./editClient');
const GetClientsList = require('./getClientsList');

module.exports = {
  CreateClient,
  UpdateClient,
  DeleteClient,
  GetClientsList,
};
